{% import '_includes/forms.twig' as forms %}
{{ forms.textField({
    label: 'tracking.gtm.config.containerId..label'|t('beacon'),
    name: 'config[containerId]',
    value: script.config.containerId ?? '',
    placeholder: 'tracking.gtm.config.containerId..placeholder'|t('beacon'),
    required: true,
    errors: script.getErrors('config.containerId'),
    first: true,
}) }}
