{% import '_includes/forms.twig' as forms %}
{{ forms.textField({
    label: 'tracking.facebookPixel.config.pixelId..label'|t('beacon'),
    name: 'config[pixelId]',
    value: script.config.pixelId ?? '',
    placeholder: 'tracking.facebookPixel.config.pixelId..placeholder'|t('beacon'),
    required: true,
    errors: script.getErrors('config.pixelId'),
    first: true,
}) }}
