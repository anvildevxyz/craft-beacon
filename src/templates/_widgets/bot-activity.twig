{% if bots is empty %}
    <p>{{ 'widgets.botActivity.no.ai.bot.activity.time.text'|t('beacon') }}</p>
{% else %}
    <table class="data fullwidth">
        <thead>
            <tr>
                <th>{{ 'widgets.botActivity.bot.text'|t('beacon') }}</th>
                <th>{{ 'widgets.botActivity.hits.text'|t('beacon') }}</th>
                <th>{{ 'widgets.botActivity.last.seen.text'|t('beacon') }}</th>
                <th>{{ 'widgets.botActivity.top.paths.text'|t('beacon') }}</th>
            </tr>
        </thead>
        <tbody>
        {% for bot in bots %}
            <tr>
                <td><strong>{{ bot.name }}</strong></td>
                <td>{{ bot.hits|number }}</td>
                <td>{{ bot.lastSeen|datetime('short') }}</td>
                <td>
                    <ul class="beacon-widget-path-list">
                    {% for p in bot.paths %}
                        <li>{{ p.path }} ({{ p.hits }})</li>
                    {% endfor %}
                    </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>
{% endif %}

{% if silentBots %}
    <p class="beacon-widget-footnote">
        <small>{{ 'widgets.botActivity.no.activity.text'|t('beacon') }}: {{ silentBots|join(', ') }}</small>
    </p>
{% endif %}
